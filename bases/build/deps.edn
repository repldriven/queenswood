;; Protobuf generation for the schema bricks' :deps/prep-lib. The uberjar
;; is mono's, through deps/mono-build.
{:deps {io.github.clojure/tools.build {:mvn/version "0.10.14"
                                       :exclusions [org.slf4j/slf4j-nop]}}
 :paths ["src"]}
