// The brand is configuration. Everything customer-facing that names the
// bank reads from here, so a rebrand is a change to this file, the
// manifest and the tokens at the top of styles.css, and nothing else.
export const brand = {
  name: "Xepha",
  legalName: "Xepha Bank Ltd",
};
