- kind:
    webhook-endpoint: {}
  bound:
    kind:
      max:
        aggregate:
          kind:
            count:
              value: 2
              window: !keyword time-window-instant
  reason: "Micro tier - max 2 webhook endpoints"
